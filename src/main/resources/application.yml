server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://15.206.91.143:5432/bloggy_db
    username: postgres
    password: rootPassword
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL10Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
